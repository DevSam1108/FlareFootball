name: tensorflow_demo
description: "Flare Football - On-Device Soccer Ball Detection POC"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.2.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  ultralytics_yolo: ^0.2.0
  audioplayers: ^6.1.0
  sensors_plus: ^6.1.0
  permission_handler: ^11.3.1
  path_provider: ^2.1.3
  share_plus: ^10.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/audio/
